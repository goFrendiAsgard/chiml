import {createServer} from "http";
import * as Koa from "koa";
import {all as koaRoute} from "koa-route";

class WebApp extends Koa {
  // TODO: extending Koa, add these methods:
  // * addMiddleware(function)
  // * addRoute(method, route, function)
  // * createServer()
}
