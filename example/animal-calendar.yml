component:

    fetchImageObj:
        ins: []
        pipe: wrapCommand
        parts: [<imageFetcherCommand>]

    getImageUrl:
        pipe: prop
        parts: [<imageKey>]

    fetchImageUrl:
        out: imageUrl
        pipe: pipeP
        parts:
            - <fetchImageObj>
            - <getImageUrl>

    fetchCalendar:
        ins: [year]
        out: calendar
        pipe: wrapCommand
        parts: [<calCommand>]

    fetchImageAndCalendar:
        pipe: parallel
        parts:
            - <fetchImageUrl>
            - <fetchCalendar>

    composeCalendar:
        ins: [imageUrl, calendar]
        out: result
        pipe: composeHtml

    main:
        pipe: pipeP
        parts:
            - <fetchImageAndCalendar>
            - <composeCalendar>

ins: [year]
out: result
bootstrap: main
